# ============================================================================
# API KEYS
# ============================================================================

# OpenAI API Key (Optional - for fallback only)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Groq API Key (RECOMMENDED - Fast & Free Tier)
# Get FREE key at: https://console.groq.com
# Free tier: 30 requests/min, 7000 tokens/min
GROQ_API_KEY=gsk_your-groq-key-here

# Cohere API Key (Optional - for embedding fallback)
# Get FREE key at: https://dashboard.cohere.com
COHERE_API_KEY=your-cohere-key-here

# ============================================================================
# SEC EDGAR CONFIGURATION (REQUIRED)
# ============================================================================

# SEC User Agent (REQUIRED by SEC EDGAR - must include email)
# Format: AppName/Version (email@example.com)
SEC_USER_AGENT=10KInsightAgent/0.1 (aditya.mishra@atidan.com)

# ============================================================================
# MODEL SELECTION
# ============================================================================

# Primary embedding provider
# Options: "sentence-transformers" (free, local), "openai", "cohere"
PRIMARY_EMBEDDING_PROVIDER=sentence-transformers

# Primary LLM provider  
# Options: "groq" (recommended, free), "openai"
PRIMARY_LLM_PROVIDER=groq

# Model strategy for handling failures
# Options: "single", "fallback", "round-robin"
MODEL_STRATEGY=fallback

# ============================================================================
# STORAGE PATHS
# ============================================================================

# Vector Store Configuration
VECTOR_STORE_DIR=src/stores/vector
CATALOG_STORE_DIR=src/stores/catalog

# Embedding Model Configuration
EMBEDDING_MODEL_NAME=text-embedding-3-large

# FastAPI Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging Level
LOG_LEVEL=INFO
